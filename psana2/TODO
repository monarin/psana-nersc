Updates 09/25/2023
- psplot_latest (give demo to the scientists, and change name to psplot_live)
- rix piranha (offsite/on) use fzp opal node instead + more rix timing to the boss
- live mode sleep problem (don't know how to fix this, but we should try)
- high rate encoder (onsite: waiting for hardware)
  - git clone slaclab/high-rate-encoder-dev
  - ./scripts/devGui --help
    ./scripts/devGui --standAloneMode 1
  - copy Epix100.cc to EncoderHR.cc
  - copy piranha_config.py to encoderhr_config.py 
    write a standalone rogue python script that set groupca (groupca DAQ:NEH 11 6)
- epixM (onsite: get the detector from dionisio)
- more timing fibers for ric epixhr emulation
- destination callback (conflicts with integrating detector?). work with silke,andy@txi

Spinifel
- fsc stand-alone script
- chimera wiki
- mpi multiple conformation

Prometheus 
MPIDataSource
- Monitor an exp/run for multiple segments
DrpDataSource
- Monitor each segment of an exp/run

hdf5 VDS path correction and sorting event timestamp

