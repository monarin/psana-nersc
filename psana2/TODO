01/21/2025
k-microscope
- Implementing KMicroscope.cc from BldDetector.cc 
  - Bld::next() replaces recv and populate variables with data received from cb_dld_event callback
  - Pgp::worker replaces timestamp[i] == ttv with PulseId matching (might have to fake one to match since I still don't understand PulseId output from cb_dld_event.
